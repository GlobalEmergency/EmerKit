import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Global Emergency Brand
  static const primary = Color(0xFF0369A1); // Azul GE
  static const primaryLight = Color(0xFF3B82F6);
  static const primaryDark = Color(0xFF0F172A); // Slate dark
  static const accent = Color(0xFFF97316); // Naranja GE
  static const accentDark = Color(0xFFEA580C);

  // Categorías
  static const soporteVital = Color(0xFFE53935);
  static const valoracion = Color(0xFF0369A1); // Azul GE
  static const signosValores = Color(0xFF10B981); // Verde GE
  static const oxigenoterapia = Color(0xFF3B82F6);
  static const tecnicas = Color(0xFFF97316); // Naranja GE
  static const proteccion = Color(0xFF8E24AA);
  static const comunicacion = Color(0xFF00897B);
  static const utilidades = Color(0xFFF59E0B);

  // Severidad
  static const severityMild = Color(0xFF10B981);
  static const severityModerate = Color(0xFFF97316);
  static const severitySevere = Color(0xFFE53935);

  // Triage
  static const triageGreen = Color(0xFF10B981);
  static const triageYellow = Color(0xFFFBBF24);
  static const triageRed = Color(0xFFE53935);
  static const triageBlack = Color(0xFF1E293B);

  // Backgrounds
  static const surfaceLight = Color(0xFFF1F5F9); // Slate 100
  static const surfaceDark = Color(0xFF0F172A); // Slate 900
}
